def is_leap(year):
    if (int(year) % 4) == 0:
        return True
    elif (int(year) % 100) ==0:
        return False
    elif (int(year) % 400) == 0:
        return True

    
    # Write your logic here

    return is_leap(year)

year = int(input())
print(is_leap(year)
